---
description: Design or review Minecraft RPG systems and verified plugin integrations through mc:rpg.
argument-hint: "[feature, plugin API, or problem]"
---

# /mc:rpg

Activate the installed `mc-rpg` skill for the `/mc:rpg` route:

<request>$ARGUMENTS</request>

Record server/plugin versions, query exact symbols from the MinecraftKit RPG catalog, and label evidence as `VERIFIED_BYTECODE`, `DERIVED_SOURCE`, `ORIGINAL_DESIGN`, or `UNVERIFIED`. Cover state, events, lifecycle, scheduler, persistence, abuse cases, degradation and test plan. Add other installed `mc-*` skills or `/mc:*` routes only for real cross-domain dependencies.
