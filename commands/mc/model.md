---
description: Design Minecraft Blockbench, 2D/3D model, rig, and animation pipelines through mc:model.
argument-hint: "[model, animation, exporter, or runtime]"
---

# /mc:model

Activate the installed `mc-model` skill for the `/mc:model` route:

<request>$ARGUMENTS</request>

Choose runtime before authoring format. Pin edition, version, loader/server-only target, exporter and runtime; define coordinates, pivots, bones, textures, animation controllers and identifier mapping. Validate transforms, culling, interpolation, hitboxes, viewer cleanup and fallback visuals.
