---
description: Build, test, package, and publish production Minecraft plugins through mc:build.
argument-hint: "[plugin build, runtime, database, or release task]"
---

# /mc:build

Activate the installed `mc-build` skill for the `/mc:build` route:

<request>$ARGUMENTS</request>

Resolve the exact Minecraft, Java, Paper/Purpur/Folia, language, storage and distribution contract. Separate official platform rules from Kit recommendations, build one reproducible artifact, test its checksum, and publish the same bytes with a compatibility matrix, changelog, SBOM and secret-scoped CI.
