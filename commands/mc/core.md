---
description: Route Minecraft platform, version, fork, loader, API, and compatibility work through mc:core.
argument-hint: "[question or task]"
---

# /mc:core

Activate the installed `mc-core` skill for the `/mc:core` route:

<request>$ARGUMENTS</request>

Resolve edition, exact Minecraft/Java version, runtime, fork/loader, proxy and client requirement. Use the pinned MinecraftKit version/source catalogs, prefer public APIs, and return the compatibility boundary plus validation plan. Route protocol, NMS, shader, pack, model or RPG details to the corresponding installed `mc-*` skill or `/mc:*` slash route when needed.
