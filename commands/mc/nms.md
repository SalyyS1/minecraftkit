---
description: Isolate and migrate Minecraft NMS, mappings, and internal APIs through mc:nms.
argument-hint: "[internal capability, version, or migration]"
---

# /mc:nms

Activate the installed `mc-nms` skill for the `/mc:nms` route:

<request>$ARGUMENTS</request>

First prove a stable API or protocol wrapper cannot provide the capability. Pin exact runtime, Java version and mapping namespace; use supported paperweight/userdev or version-module boundaries and fail closed on unknown signatures. Include compile/runtime tests per supported version and never redistribute Minecraft binaries.
