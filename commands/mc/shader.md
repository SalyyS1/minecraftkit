---
description: Build, debug, or review Minecraft shaders and render pipelines through mc:shader.
argument-hint: "[effect, shader, renderer, or problem]"
---

# /mc:shader

Activate the installed `mc-shader` skill for the `/mc:shader` route:

<request>$ARGUMENTS</request>

Distinguish vanilla core-shader overrides, mod render APIs and Iris shader packs. Pin Minecraft version, loader/renderer, render stage and pack stack; identify programs, attributes, uniforms, samplers and GPU state. Treat core shader contracts as version-specific and include compile, conflict, hardware and fallback validation.
