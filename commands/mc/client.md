---
description: Design client mods or server-driven client projections through mc:client.
argument-hint: "[client-side feature or problem]"
---

# /mc:client

Activate the installed `mc-client` skill for the `/mc:client` route:

<request>$ARGUMENTS</request>

State whether an installed client mod is required or the feature targets vanilla-client projection. Pin Java/Bedrock clients and server/proxy versions; separate authoritative server state from per-viewer visuals. Cover join, respawn, world change, tracking, interaction, bandwidth, cleanup and Bedrock degradation.
