# Notice, Provenance, And Limits

Salyyy Minecraft Kit is authored and maintained under the nametag **SalyVn / Salyyy**. MinecraftKit is an independent research and engineering artifact. Minecraft, plugin, mod, library, and product names and trademarks belong to their owners.

The kit contains factual metadata, public API symbol indexes, original summaries, architecture research, compatibility warnings, and independently designed feature compositions. It deliberately excludes:

- Mojang client/server JARs, mappings bodies, shaders, textures, models, sounds, fonts, or other game assets;
- plugin/mod JARs, class files, copied implementation bodies, or substantial source excerpts;
- proprietary/default/generated vendor packs and marketplace assets;
- credentials, tokens, account/session data, logs, caches, histories, or private runtime data;
- the raw CursorCs JAR, decompiled source/resources, marketplace token, or reconstructed implementation.

The upstream registry records canonical links, exact commit observations, release metadata, GitHub license metadata, priorities, domains, ingestion policies, and original routing notes. A repository being public does not make every file open source or redistributable. GitHub's license descriptor may omit inherited, per-file, generated-data, documentation, asset, custom, or commercial terms.

`index`, `derive`, `metadata-only`, and `link-only` are MinecraftKit research controls, not legal conclusions. Before redistribution, commercial operation, source reuse, or bundling, review every applicable upstream license and term. Mojang content remains governed by the Minecraft EULA and Usage Guidelines. This notice is not legal advice.

The authorized RPG catalog was produced from locally supplied binaries/decompiled output. Decompilation can distort control flow, generics, annotations, nullability, synthetic constructs, and line mappings. `VERIFIED_BYTECODE` describes only the supplied artifact. `DERIVED_SOURCE` is independent inference, not a vendor guarantee. Critical behavior requires a minimal consumer compile and authorized target-server test.

The CursorCs clean-room report records file/tool hashes, public descriptor/API signatures, architecture and original behavioral summaries. Two obfuscated methods were not decompiled; affected conclusions remain qualified. No raw output is part of this repository or release payload.

Source inventory hashes and upstream commit pins exist for reproducibility and drift detection. They do not grant rights to the referenced content.
